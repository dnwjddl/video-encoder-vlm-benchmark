[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vlm-encoder-benchmark"
version = "0.1.0"
description = "Controlled frozen visual encoder comparison for video-language benchmarks."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
